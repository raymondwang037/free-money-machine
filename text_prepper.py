import re

# read the files
# remove the separators
# every sentence is its own list element
# if two consecutive sentences are < 200 characters join
# if sentence is greater than 200 characters count commas
# Should be the start of an array element, going until the first comma
# if two commas, have three separate array elements 100%%%%
# do we export to a new text file which each array element as a new line?
